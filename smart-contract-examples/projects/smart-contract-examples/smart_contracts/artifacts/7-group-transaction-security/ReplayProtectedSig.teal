#pragma version 11
#pragma typetrack false

// smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts::program() -> uint64:
main:
    intcblock 1 TMPL_EXPIRATION_ROUND
    bytecblock TMPL_LEASE
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15
    // Txn.typeEnum === TransactionType.Payment &&
    txn TypeEnum
    intc_0 // 1
    ==
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-16
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:16
    // Txn.amount <= Uint64(1_000_000) &&
    txn Amount
    pushint 1000000 // 1000000
    <=
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-16
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:17
    // Txn.fee <= Global.minTxnFee &&
    txn Fee
    global MinTxnFee
    <=
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-17
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:18
    // Txn.rekeyTo === Global.zeroAddress &&
    txn RekeyTo
    global ZeroAddress
    ==
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-18
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee &&
    // Txn.rekeyTo === Global.zeroAddress &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:19
    // Txn.closeRemainderTo === Global.zeroAddress &&
    txn CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-19
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:21
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    txn Lease
    bytec_0 // TMPL_LEASE
    ==
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-21
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    // // Lease + LastValid bound = at most one transaction per window
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    bz main_bool_false@8
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:22
    // Txn.lastValid <= TemplateVar<uint64>("EXPIRATION_ROUND")
    txn LastValid
    intc_1 // TMPL_EXPIRATION_ROUND
    <=
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:15-22
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    // // Lease + LastValid bound = at most one transaction per window
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    // Txn.lastValid <= TemplateVar<uint64>("EXPIRATION_ROUND")
    bz main_bool_false@8
    intc_0 // 1
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:14-23
    // return (
    //   Txn.typeEnum === TransactionType.Payment &&
    //   Txn.amount <= Uint64(1_000_000) &&
    //   Txn.fee <= Global.minTxnFee &&
    //   Txn.rekeyTo === Global.zeroAddress &&
    //   Txn.closeRemainderTo === Global.zeroAddress &&
    //   // Lease + LastValid bound = at most one transaction per window
    //   Txn.lease === TemplateVar<bytes>("LEASE") &&
    //   Txn.lastValid <= TemplateVar<uint64>("EXPIRATION_ROUND")
    // );
    return

main_bool_false@8:
    pushint 0 // 0
    // smart_contracts/7-group-transaction-security/replay-protected-sig.algo.ts:14-23
    // return (
    //   Txn.typeEnum === TransactionType.Payment &&
    //   Txn.amount <= Uint64(1_000_000) &&
    //   Txn.fee <= Global.minTxnFee &&
    //   Txn.rekeyTo === Global.zeroAddress &&
    //   Txn.closeRemainderTo === Global.zeroAddress &&
    //   // Lease + LastValid bound = at most one transaction per window
    //   Txn.lease === TemplateVar<bytes>("LEASE") &&
    //   Txn.lastValid <= TemplateVar<uint64>("EXPIRATION_ROUND")
    // );
    return
