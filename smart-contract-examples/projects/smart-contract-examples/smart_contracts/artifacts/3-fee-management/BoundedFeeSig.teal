#pragma version 11
#pragma typetrack false

// smart_contracts/3-fee-management/fee-bounding.algo.ts::program() -> uint64:
main:
    intcblock 1 TMPL_FIRST_VALID TMPL_LAST_VALID
    bytecblock TMPL_INTENDED_RECEIVER TMPL_LEASE
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23
    // Txn.typeEnum === TransactionType.Payment &&
    txn TypeEnum
    intc_0 // 1
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-24
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:24
    // Txn.amount <= Uint64(1_000_000) &&
    txn Amount
    pushint 1000000 // 1000000
    <=
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-24
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:25
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    txn Fee
    global MinTxnFee
    <=
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-25
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:26
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    txn Receiver
    bytec_0 // TMPL_INTENDED_RECEIVER
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-26
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:27
    // Txn.rekeyTo === Global.zeroAddress &&
    txn RekeyTo
    global ZeroAddress
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-27
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    // Txn.rekeyTo === Global.zeroAddress &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:28
    // Txn.closeRemainderTo === Global.zeroAddress &&
    txn CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-28
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:30
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    txn Lease
    bytec_1 // TMPL_LEASE
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-30
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    // // Lease + exact FirstValid/LastValid = at most one execution
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:31
    // Txn.firstValid === TemplateVar<uint64>("FIRST_VALID") &&
    txn FirstValid
    intc_1 // TMPL_FIRST_VALID
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-31
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    // // Lease + exact FirstValid/LastValid = at most one execution
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    // Txn.firstValid === TemplateVar<uint64>("FIRST_VALID") &&
    bz main_bool_false@10
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:32
    // Txn.lastValid === TemplateVar<uint64>("LAST_VALID")
    txn LastValid
    intc_2 // TMPL_LAST_VALID
    ==
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:23-32
    // Txn.typeEnum === TransactionType.Payment &&
    // Txn.amount <= Uint64(1_000_000) &&
    // Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    // Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    // Txn.rekeyTo === Global.zeroAddress &&
    // Txn.closeRemainderTo === Global.zeroAddress &&
    // // Lease + exact FirstValid/LastValid = at most one execution
    // Txn.lease === TemplateVar<bytes>("LEASE") &&
    // Txn.firstValid === TemplateVar<uint64>("FIRST_VALID") &&
    // Txn.lastValid === TemplateVar<uint64>("LAST_VALID")
    bz main_bool_false@10
    intc_0 // 1
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:22-33
    // return (
    //   Txn.typeEnum === TransactionType.Payment &&
    //   Txn.amount <= Uint64(1_000_000) &&
    //   Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    //   Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    //   Txn.rekeyTo === Global.zeroAddress &&
    //   Txn.closeRemainderTo === Global.zeroAddress &&
    //   // Lease + exact FirstValid/LastValid = at most one execution
    //   Txn.lease === TemplateVar<bytes>("LEASE") &&
    //   Txn.firstValid === TemplateVar<uint64>("FIRST_VALID") &&
    //   Txn.lastValid === TemplateVar<uint64>("LAST_VALID")
    // );
    return

main_bool_false@10:
    pushint 0 // 0
    // smart_contracts/3-fee-management/fee-bounding.algo.ts:22-33
    // return (
    //   Txn.typeEnum === TransactionType.Payment &&
    //   Txn.amount <= Uint64(1_000_000) &&
    //   Txn.fee <= Global.minTxnFee && // Added: caps fee to prevent draining
    //   Txn.receiver === TemplateVar<Account>("INTENDED_RECEIVER") &&
    //   Txn.rekeyTo === Global.zeroAddress &&
    //   Txn.closeRemainderTo === Global.zeroAddress &&
    //   // Lease + exact FirstValid/LastValid = at most one execution
    //   Txn.lease === TemplateVar<bytes>("LEASE") &&
    //   Txn.firstValid === TemplateVar<uint64>("FIRST_VALID") &&
    //   Txn.lastValid === TemplateVar<uint64>("LAST_VALID")
    // );
    return
